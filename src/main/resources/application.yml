server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/javico92/configuration

  # Spring properties in case you use different profiles
  profiles:
    active: dev